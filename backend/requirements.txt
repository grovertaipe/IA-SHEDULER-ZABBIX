# zabbix-ai-maintenance-v2 backend dependencies (pinned versions, Req 1.6).
# Legacy pins preserved in requirements.legacy.txt.

# --- Runtime ---
flask==3.0.3
flask-cors==4.0.1
requests==2.32.3
gunicorn==22.0.0

# --- AI providers ---
google-genai==1.31.0
openai==1.45.0
boto3==1.40.30

# --- JSON Schema validation (Req 29) ---
jsonschema==4.23.0

# --- Observability / metrics (Req 30) ---
prometheus-client==0.21.0

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
hypothesis==6.112.1

# --- Linting & typing ---
ruff==0.6.9
mypy==1.11.2
