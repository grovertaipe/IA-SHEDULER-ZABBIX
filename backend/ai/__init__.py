"""AI provider abstraction (provider-agnostic interface + implementations).

Includes the common ``AIProvider`` interface, Gemini/OpenAI implementations,
failover orchestration, JSON schema validation, the provider factory and the
externalized prompt. Populated in later tasks.
"""
